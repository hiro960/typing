name: chaletta
description: "韓国語タイピングと文章作成を楽しく学べる学習アプリ。仲間と一緒に上達しよう！"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
  
#   【チャレッタ】
#    韓国語の学習を、もっと楽しく、効果的に。
# 
#    タイピング練習からTOPIK対策まで。
#    あなたの韓国語学習をサポートするオールインワンアプリ。

#   【こんな方におすすめ】
#   ・韓国語の入力に慣れたい方
#   ・韓国語で文章を書けるようになりたい方
#   ・独学でモチベーションが続かない方
#   ・他の学習者と交流しながら学びたい方

# 機能一覧
#  ・カナダラ表
#  ・瞬間作文
#  ・書き取りレッスン
#  ・タイピングゲーム
#  ・発音ゲーム
#  ・文法辞典
#  ・漢字語
#  ・翻訳
#  ・日記（SNS）
#  ・単語帳

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.1+1

# version 1.1 発音ゲームの追加
# version 1.2 翻訳機能の追加

environment:
  sdk: ^3.9.2
  flutter: ">=3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  in_app_purchase: ^3.2.3

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  iconsax_flutter: ^1.0.1
  forui: ^0.16.0
  auth0_flutter: ^1.14.0
  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4
  flutter_dotenv: ^6.0.0
  json_annotation: ^4.9.0
  flutter_riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3
  riverpod: ^3.0.3
  shared_preferences: ^2.3.2
  characters: ^1.4.0
  confetti: ^0.7.0
  freezed_annotation: ^3.0.0
  flutter_tts: ^4.0.2
  uuid: ^4.5.1
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2
  firebase_core: ^3.15.2
  firebase_messaging: ^15.0.2
  flutter_launcher_icons: ^0.14.4
  fl_chart: ^1.1.1
  share_plus: ^12.0.1
  url_launcher: ^6.3.1
  speech_to_text: ^7.0.0
  deepl_dart: ^2.0.0
  flutter_native_splash: ^2.4.7
  audioplayers: ^6.1.0
  package_info_plus: ^8.0.0
  app_badge_plus: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.11.1
  riverpod_generator: ^3.0.3
  riverpod_lint: ^3.0.3
  custom_lint:
  freezed: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/se/
    - assets/lessons/
    - assets/words/
    - assets/writing/topik/
    - assets/writing/beginner/
    - assets/writing/travel/
    - assets/writing/hobby/
    - assets/ranking_game/words/
    - assets/grammar/
    - assets/grammar/adnominal/
    - assets/grammar/conjugation/
    - assets/grammar/connective/
    - assets/grammar/expression/
    - assets/grammar/orthography/
    - assets/grammar/particle/
    - assets/grammar/quotation/
    - assets/grammar/sentence_ending/
    - assets/grammar/substantive/
    - assets/grammar/tense_aspect/
    - assets/grammar/word_formation/
    - assets/hanja/characters/
    - assets/hanja/words/
    - assets/quick_translation/
    - assets/quick_translation/particle/
    - assets/quick_translation/conjugation/
    - assets/quick_translation/connective/
    - assets/quick_translation/sentence_ending/
    - assets/quick_translation/tense_aspect/
    - assets/quick_translation/expression/
    - assets/quick_translation/adnominal/
    - assets/quick_translation/substantive/
    - assets/quick_translation/quotation/
    - assets/quick_translation/word_formation/
    - assets/shadowing/
    - assets/audio/shadowing/beginner/
    - assets/audio/shadowing/intermediate/
    - assets/audio/shadowing/advanced/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_icons: 
  image_path_android: assets/launcher_icon/android_icon.png
  adaptive_icon_background: '#2DBFBF' #iconの背景色
  adaptive_icon_foreground: assets/launcher_icon/android_icon.png
  image_path_ios: assets/launcher_icon/ios_icon.png
  android: true
  adaptive_icon_padding: true
  min_sdk_android: 30 #Androidの最小SDKバージョン
  ios: true   

flutter_native_splash:
  color: "#2DBFBF"
  image: assets/launcher_icon/ios_icon.png