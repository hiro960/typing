# ホーム画面 UI/UX 改善計画

## 概要

メニューが増えてわかりづらくなったホーム画面を、**シングルスクロール + 3カテゴリ構成**に再設計し、全10機能へのアクセスを改善する。

## 現状の課題

| 課題 | 詳細 |
|------|------|
| タブの分散 | 4タブ（タイピング/ゲーム/書き取り/瞬間作文）に機能が分散 |
| アクセス階層が深い | タブ → アコーディオン → レッスン選択（最短3タップ） |
| 辞典の発見性が低い | 文法辞典・漢字語辞典がヘッダーの小さなアイコンのみ |

## 改善方針

### 新しいレイアウト構造

```
┌─────────────────────────────────────┐
│  Header: 안녕하세요 + [設定]         │
├─────────────────────────────────────┤
│  Quick Tools Card（為替 + 翻訳）     │
├─────────────────────────────────────┤
│  Weekly Progress Summary             │
│  ◆◆◆◇◇◇◇  3日連続! 🔥           │
├─────────────────────────────────────┤
│  ★ おすすめ: タイピング練習          │  ← 目立つ位置に配置
│  ┌────────────────────────────┐     │
│  │ 初級 / 中級 / 上級 選択    │     │
│  └────────────────────────────┘     │
├─────────────────────────────────────┤
│  ◆ 学ぶ (Learn)                     │
│  ┌───────┐ ┌───────┐ ┌───────┐     │
│  │カナダラ│ │瞬間作文│ │書き取り│     │
│  └───────┘ └───────┘ └───────┘     │
├─────────────────────────────────────┤
│  ◆ 遊ぶ (Play)                      │
│  ┌───────┐ ┌───────┐ ┌───────┐     │
│  │タイピ │ │発音   │ │漢字語 │     │
│  │ゲーム │ │ゲーム │ │クイズ │     │
│  └───────┘ └───────┘ └───────┘     │
├─────────────────────────────────────┤
│  ◆ 調べる (Reference)               │
│  ┌───────┐ ┌───────┐               │
│  │文法   │ │漢字語 │               │
│  │辞典   │ │辞典   │               │
│  └───────┘ └───────┘               │
├─────────────────────────────────────┤
│  [AI先生に聞く]                      │
├─────────────────────────────────────┤
│  Bottom Nav: ホーム|日記|単語帳|通知|Profile │
└─────────────────────────────────────┘
```

### 機能の配置

| カテゴリ | 機能 | 説明 |
|---------|------|------|
| **おすすめ（上部強調）** | タイピング練習 | メイン学習機能として目立つカードで表示 |
| **学ぶ** | カナダラ表、瞬間作文、書き取りレッスン | 体系的な学習コンテンツ |
| **遊ぶ** | タイピングゲーム、発音ゲーム、漢字語クイズ | ゲーミフィケーション |
| **調べる** | 文法辞典、漢字語辞典 | リファレンス・辞書 |
| **ボトムナビ（維持）** | 日記、単語帳 | 既存の配置を維持 |

---

## 実装計画

### Phase 1: 新規コンポーネント作成

#### 1.1 FeatureCard ウィジェット作成

**ファイル**: `application/lib/ui/screens/home/home_feature_card.dart`

```dart
/// 各機能へのナビゲーションカード
class FeatureCard extends StatelessWidget {
  final String title;
  final String? subtitle;
  final IconData icon;
  final Color color;
  final VoidCallback onTap;
  final Widget? badge; // 進捗やNew表示用
}
```

- タッチターゲット: 最小48x48px
- 3列グリッドで配置
- InkWellでタップフィードバック

#### 1.2 CategorySection ウィジェット作成

**ファイル**: `application/lib/ui/screens/home/home_category_section.dart`

```dart
/// カテゴリセクション（学ぶ/遊ぶ/調べる）
class CategorySection extends StatelessWidget {
  final String title;
  final List<FeatureCard> features;
}
```

#### 1.3 TypingPracticeHero ウィジェット作成

**ファイル**: `application/lib/ui/screens/home/home_typing_hero.dart`

```dart
/// タイピング練習への目立つエントリーポイント
class TypingPracticeHero extends ConsumerWidget {
  // 初級/中級/上級の選択UI
  // 進捗表示
}
```

#### 1.4 WeeklyProgressCard ウィジェット作成

**ファイル**: `application/lib/ui/screens/home/home_weekly_progress.dart`

```dart
/// 週間学習進捗のサマリー表示
class WeeklyProgressCard extends ConsumerWidget {
  // 連続学習日数ドット表示
  // 各カテゴリの学習時間
}
```

### Phase 2: home_screen.dart の構造変更

**ファイル**: `application/lib/ui/screens/home/home_screen.dart`

#### 2.1 削除するもの
- `_selectedTab` 状態管理
- `_LearningTabSelector` の使用
- タブ別のコンテンツビルダー（`_TypingTabContent`, `_GamesTabContent`, etc.）

#### 2.2 新しい構造

```dart
CustomScrollView(
  slivers: [
    // 1. Quick Tools (既存維持)
    SliverToBoxAdapter(child: _QuickToolsCard(...)),

    // 2. Weekly Progress (新規)
    SliverToBoxAdapter(child: WeeklyProgressCard()),

    // 3. タイピング練習 Hero (新規・目立つ配置)
    SliverToBoxAdapter(child: TypingPracticeHero()),

    // 4. 学ぶセクション
    SliverToBoxAdapter(child: CategorySection(
      title: '学ぶ',
      features: [カナダラ表, 瞬間作文, 書き取り],
    )),

    // 5. 遊ぶセクション
    SliverToBoxAdapter(child: CategorySection(
      title: '遊ぶ',
      features: [タイピングゲーム, 発音ゲーム, 漢字語クイズ],
    )),

    // 6. 調べるセクション
    SliverToBoxAdapter(child: CategorySection(
      title: '調べる',
      features: [文法辞典, 漢字語辞典],
    )),

    // 7. AI先生 (既存維持)
    SliverToBoxAdapter(child: AiGradientButton(...)),
  ],
)
```

### Phase 3: 不要ファイルの削除・整理

#### 3.1 削除対象
- `home_learning_tabs.dart` - タブ関連のコード削除

#### 3.2 移行・整理
- `home_level_accordions.dart` → `TypingPracticeHero` に統合
- `home_writing_accordions.dart` → 遷移先画面として維持（ホームからは FeatureCard で遷移）

### Phase 4: ヘッダーの簡素化

**変更内容**:
- 辞典アイコン（翻訳、文法）を削除
- 設定アイコンのみ残す

```dart
// Before
actions: [
  FHeaderAction(icon: Icons.translate_outlined, ...),  // 削除
  FHeaderAction(icon: Icons.menu_book_outlined, ...),  // 削除
  FHeaderAction(icon: Icons.settings_outlined, ...),   // 維持
]

// After
actions: [
  FHeaderAction(icon: Icons.settings_outlined, onPress: _openSettings),
]
```

---

## 変更対象ファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `home_screen.dart` | 構造変更（タブ廃止、カテゴリ構成へ） |
| `home_learning_tabs.dart` | 削除 |
| `home_feature_card.dart` | 新規作成 |
| `home_category_section.dart` | 新規作成 |
| `home_typing_hero.dart` | 新規作成 |
| `home_weekly_progress.dart` | 新規作成 |
| `home_level_accordions.dart` | TypingPracticeHeroに統合後、削除検討 |

---

## 期待効果

| 指標 | Before | After |
|------|--------|-------|
| 機能発見性 | 低（タブ切替必要） | 高（一覧表示） |
| アクセスタップ数 | 平均3タップ | 平均1-2タップ |
| 辞典へのアクセス | ヘッダーアイコン（気づきにくい） | メインカード（発見しやすい） |
| 学習モチベーション | 進捗が分散 | 週間進捗で可視化 |

---

## デザイン仕様

### 色彩
- 背景: `#080B12`（既存維持）
- カテゴリアクセント:
  - 学ぶ: `#5AA2FF`（primaryBright）
  - 遊ぶ: `#27C3FF`（secondary）
  - 調べる: `#30A8FF`（accentEnd）

### タイポグラフィ
- セクションタイトル: 16px SemiBold
- カードタイトル: 14px Medium
- カードサブタイトル: 12px Regular

### スペーシング
- セクション間: 24px
- カード間: 12px
- カード内パディング: 16px
