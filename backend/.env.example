# データベース設定
# ローカル開発環境用（Docker）
DATABASE_URL="postgresql://korean_typing_user:korean_typing_password@localhost:5432/korean_typing_db?schema=public"

# PostgreSQL設定（docker-compose.yml用）
POSTGRES_USER=korean_typing_user
POSTGRES_PASSWORD=korean_typing_password
POSTGRES_DB=korean_typing_db

# Auth0設定
# https://manage.auth0.com/ から取得

# AUTH0_ISSUER_BASE_URL（必須）
# Auth0のテナントURL（末尾スラッシュあり・なしどちらでもOK）
# 例: https://your-tenant.us.auth0.com または https://your-tenant.us.auth0.com/
AUTH0_ISSUER_BASE_URL=https://your-tenant.us.auth0.com

# AUTH0_AUDIENCE（推奨）
# Auth0でAPIを作成した場合のAPI Identifier
# 例: https://your-api.example.com
#
# ⚠️ 重要: この値を設定しないと、Auth0が暗号化トークン(JWE)を発行し、
# バックエンドの認証が失敗します(401エラー)。
#
# 設定手順:
# 1. Auth0ダッシュボードで Applications -> APIs -> Create API
# 2. Name: 任意 (例: Korean Typing API)
# 3. Identifier: https://api.korean-typing.example.com (この値を以下に設定)
# 4. Signing Algorithm: RS256
# 5. この値をFlutterの.envファイルにも設定
#
# 注意: Auth0の管理API (/api/v2/) は使用しないでください
AUTH0_AUDIENCE=

# Next.js設定
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000

# Vercel Blob設定（本番環境用）
# BLOB_READ_WRITE_TOKEN=your-blob-token

# Pusher設定（リアルタイム通知用）
# NEXT_PUBLIC_PUSHER_APP_KEY=your-pusher-app-key
# PUSHER_APP_ID=your-pusher-app-id
# PUSHER_SECRET=your-pusher-secret
# PUSHER_CLUSTER=your-pusher-cluster

# Redis設定（Upstash - 本番環境用）
# REDIS_URL=your-redis-url
# REDIS_TOKEN=your-redis-token

APPLE_SHARED_SECRET=''